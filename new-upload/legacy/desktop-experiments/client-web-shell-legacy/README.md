This directory preserves the pre-normalization Electron shell that used to live inside `apps/client-web`.

It is kept for historical reference only and is not part of the default build or acceptance path.

Use `apps/client-desktop` for the active desktop packaging flow.
