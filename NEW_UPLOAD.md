# 新上传说明

> 上传时间：2026-05-01

本目录下的 `new-upload/` 文件夹包含了从本地开发环境新上传的完整项目源码。

## 说明

- **本次上传不会覆盖仓库中现有的任何子文件夹**（如 `apps`、`docs`、`infra`、`legacy`、`packages` 等）。
- 新内容统一存放在 `new-upload/` 目录下，与现有代码隔离。
- 中间产物（`node_modules`、`dist`、`build`、日志、临时脚本、压缩包等）已排除，仅保留源码与必要资源。

## 待办

请协作者（@tomfng 或其他维护者）根据实际情况进行：
- 对比 `new-upload/` 与现有目录的差异
- 手动合并或替换需要更新的模块
- 合并完成后可删除 `new-upload/` 或保留作为归档

---
*由本地开发环境自动上传，等待人工审阅与合并。*
