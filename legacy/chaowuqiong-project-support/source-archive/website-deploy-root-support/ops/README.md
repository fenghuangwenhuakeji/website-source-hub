# Ops Layout

## Purpose

This folder collects operational artifacts that were previously scattered in the root of `D:\网站部署`.

## Current Structure

- `sql/`
  - loose SQL patches and reset scripts
- `nginx/`
  - loose Nginx and site config files
- `logs/`
  - temporary execution logs
- `scripts/deploy-legacy/`
  - older deployment and restart scripts
- `scripts/mysql/`
  - MySQL and DB helper scripts
- `scripts/diagnostics/`
  - diagnostics and verification helpers
- `scripts/tests/`
  - old test scripts and debug probes
- `scripts/business-patches/`
  - one-off business patch scripts
- `scripts/scratch/`
  - scratch and orphaned utility files

## Note

The currently known-good cloud path is no longer stored here. It lives in:

- `D:\网站部署\cloud-bridge`
