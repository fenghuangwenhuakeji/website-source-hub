---
name: site-deployment-catalog
description: Catalog and route the entire imported D:\网站部署 collection inside Codex. Use when you need the big-picture map of imported Trae skills, Agent+Agent libraries, generated agent packs, project-specific agent families, MCP source, llm-proxy source, or system docs.
---

# Site Deployment Catalog

Use these files first:

- `D:\网站部署\codex\IMPORT_INVENTORY.md`
- `D:\网站部署\codex\vendor_imports\site-deployment\site-deployment-manifest.json`
- `D:\网站部署\codex\vendor_imports\site-deployment\site-deployment-skill-index.json`

Route work like this:

1. Use `site-deployment-direct-skills` for direct imported agents and top-level skill families.
2. Use `site-deployment-generated-libraries` for large generated agent packs and massive themed libraries.
3. Use `site-deployment-system-docs` for Trae portable docs, startup scripts, orchestration scripts, and architecture notes.
4. Use `site-deployment-mcp-source` for the 超无穹 CodeEditor MCP source.
5. Use `site-deployment-llm-proxy` for the Express LLM proxy route.
6. Use `site-deployment-skill-finder` when you need to locate any exact imported skill by name or partial path.

Preserve the original mounted path in follow-up work.
